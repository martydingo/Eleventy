apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eleventy.fullname" . }}-test-connection"
  labels:
    {{- include "eleventy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eleventy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
